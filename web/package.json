{
  "name": "listening-web",
  "version": "1.0.0",
    "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview"
    "dev":   "vite",
    "build": "vite build",
    "start": "serve -s dist -l $PORT"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^3.0.0",
    "vite": "^4.0.0"
  }
}
